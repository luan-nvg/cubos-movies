import styled from "styled-components"

export const CombinedInputGrid = styled.div`
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  justify-content: center;
  border-radius: 5px;
`

export const CombinedChannelInputComponent = styled.div`
  display: flex;
  flex-direction: column;
  align-items: center;
  margin-top: 5px;
`
